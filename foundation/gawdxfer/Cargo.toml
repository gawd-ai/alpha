[package]
name = "gawdxfer"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "The GAWD bulk-transfer contract. Carries the GX chunked/resumable wire vocabulary, binary chunk frame, chunk math, and streaming SHA-256 helpers shared by transports and surfaces."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
