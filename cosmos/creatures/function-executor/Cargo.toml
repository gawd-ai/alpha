[package]
name = "function-executor"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Reference Realm-local function executor with durable attempt claims and terminal receipt deduplication."

[dependencies]
aether = { path = "../../aether" }
gawdfn = { path = "../../../foundation/gawdfn" }
function-home = { path = "../function-home" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
function-home = { path = "../function-home", features = ["durability-test-hooks"] }
