[package]
name = "omega-federator"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "The Omega federation creature: cross-Realm routing, pull-based registry anti-entropy, signed reputation propagation over the SEER consensus topic, and the cross-Realm quarantine path. Filled into Role::OMEGA_GATEWAY. Not substrate — operators bind their own."

[dependencies]
aether = { path = "../../aether" }
bestiary = { path = "../../bestiary" }
seer = { path = "../../seer" }
sigil = { path = "../../sigil" }
# The `omega` concept crate owns the gateway socket name (`omega::GATEWAY_ROLE`) this
# federator binds. (Omega intentionally has no routing seam trait — see the crate docs.)
omega = { path = "../../omega" }
registry-mem = { path = "../registry-mem" }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
