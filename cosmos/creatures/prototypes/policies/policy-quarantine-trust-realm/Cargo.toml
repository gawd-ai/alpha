[package]
name = "policy-quarantine-trust-realm"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Reference injected QuarantineTrust: honors an inbound cross-Realm quarantine notice only when at least one attesting peer is in a per-Realm trusted set. A realistic reference for immune-response's trust socket (T2) — realm-scoped so a peer trusted in one Realm can't quarantine creatures in another. Not substrate."

[dependencies]
aether = { path = "../../../../aether" }
immune-response = { path = "../../../../creatures/immune-response" }
