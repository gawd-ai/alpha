[package]
name = "policy-prefer-promoted"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Reference injected admission policy: admits a creature only when the registry carries a verified, at-or-above-threshold fitness promotion for its (realm, artifact_hash). Proves that selection pressure is a POLICY choice, not a substrate gate (T7) — the contrast with AllowAll is the whole point. Not substrate."

[dependencies]
sanctum = { path = "../../../../sanctum" }
registry-mem = { path = "../../../../creatures/registry-mem" }
sigil = { path = "../../../../sigil" }
