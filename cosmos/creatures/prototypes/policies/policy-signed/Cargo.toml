[package]
name = "policy-signed"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Reference injected admission policy that requires a valid signature, an artifact-bytes hash match, and (optionally) an Abode-key allowlist. NOT substrate — operators ship their own policy creatures."

[dependencies]
sanctum = { path = "../../../../sanctum" }
sigil = { path = "../../../../sigil" }
