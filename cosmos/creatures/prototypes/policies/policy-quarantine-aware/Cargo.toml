[package]
name = "policy-quarantine-aware"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Reference injected admission policy: rejects a quarantined creature BEFORE applying the promotion gate — so a creature that is BOTH a verified promotion AND quarantined is refused. Defense overrides selection. Contrast with policy-prefer-promoted, which admits the same entry. Not substrate."

[dependencies]
sanctum = { path = "../../../../sanctum" }
registry-mem = { path = "../../../../creatures/registry-mem" }
sigil = { path = "../../../../sigil" }
