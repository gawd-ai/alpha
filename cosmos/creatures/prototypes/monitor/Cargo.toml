[package]
name = "monitor"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Reference injected creature (NOT substrate) — a 'nervous-system' monitor: subscribes to the PROPRIOCEPTION + FITNESS topics and renders the substrate's sense stream as a live one-line tape. The fabric ships the senses; this is one minimal way to read them."

[dependencies]
aether = { path = "../../../aether" }
sigil = { path = "../../../sigil" }
serde_json = { workspace = true }
