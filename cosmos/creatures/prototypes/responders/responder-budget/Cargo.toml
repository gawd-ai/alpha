[package]
name = "responder-budget"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Reference standing consumer for the SEER `budget` topic. On a grace Query it grants up to a per-request ceiling, optionally drawn from a finite depleting pool, and answers granted/denied + the units extended. NOT substrate — a starting point operators fork into their own grace model."

[dependencies]
aether = { path = "../../../../aether" }
seer = { path = "../../../../seer" }
serde = { workspace = true }
serde_json = { workspace = true }
