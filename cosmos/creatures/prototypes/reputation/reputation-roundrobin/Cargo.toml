[package]
name = "reputation-roundrobin"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Reference injected reputation weight model: every signed attestation weighs 1.0. The bare-minimum ReputationWeigher reference for omega-federator. Not substrate — operators write their own (EWMA, realm-allowlist, VRF-weighted)."

[dependencies]
aether = { path = "../../../../aether" }
omega-federator = { path = "../../../../creatures/omega-federator" }
