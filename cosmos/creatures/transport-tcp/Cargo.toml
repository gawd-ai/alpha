[package]
name = "transport-tcp"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "An authenticated TCP peer link. Mutual ed25519 challenge-response handshake against a pre-shared peer allowlist, then length-prefixed Envelope JSON over the channel. Bound to Role::TRANSPORT; routes Node(_,_)-addressed envelopes off-node."

[dependencies]
aether = { path = "../../aether" }
sigil = { path = "../../sigil" }
gawdxfer = { path = "../../../foundation/gawdxfer" }
serde = { workspace = true }
serde_json = { workspace = true }
ring = { workspace = true }
